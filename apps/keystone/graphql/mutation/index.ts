/**
 * Example (no custom mutations yet; rm when mutation added):
 *
 * import {
 *   mutationName,
 *   typedefs as mutationNameTypeDefs,
 * } from './mutationName'
 */

export const typedefs = /* GraphQL */ ``

const Mutation = {
  /* mutationName, */
  /* ...additional mutations */
}

export default Mutation
